<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>EstiloRodante</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/agregar.css">
</head>
<body class="light-mode d-flex flex-column min-vh-100">
    <%- include('../partial/header.ejs') %>
    <main class="container py-5 flex-grow-1">
    <div class="row">
      <!-- Columna izquierda: formulario -->
      <div class="col-md-4 mb-4">
        <div class="card shadow p-3">
          <h4 class="text-center mb-3">Cargar Producto</h4>
          <form id="productForm">
            <div class="mb-3">
              <label for="nombre" class="form-label">Nombre</label>
              <input type="text" id="nombre" class="form-control" required>
            </div>
            <div class="mb-3">
              <label for="descripcion" class="form-label">Descripción</label>
              <input type="text" id="descripcion" class="form-control" required>
            </div>
            <div class="mb-3">
              <label for="image" class="form-label">Imagen (URL)</label>
              <input type="url" id="image" class="form-control">
            </div>
            <div class="mb-3">
              <label for="type" class="form-label">Tipo</label>
              <select id="type" class="form-select" required>
                <option value="Cubierta">Cubierta</option>
                <option value="Rin">Rin</option>
              </select>
            </div>
            <div class="mb-3">
              <label for="price" class="form-label">Precio</label>
              <input type="number" id="price" class="form-control" required>
            </div>
            <button type="button" class="btn btn-primary w-100" onclick="updatePreview()">Cargar</button>
            <button type="button" class="btn btn-primary w-100" onclick="agregarProducto()">Agregar</button>
          </form>
        </div>
      </div>

      <!-- Columna derecha: preview -->
      <div class="col-md-8">
        <div class="card shadow p-3">
          <h4 class="text-center mb-3" id="previewNombre">Nombre del Producto</h4>
          <div class="text-center mb-3">
            <img id="previewImage" src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQumqw6UawRn7rOgAvevIfEnX55015CA-oTeA&s" class="img-fluid rounded" alt="Vista previa" style="width: 300px; height: 200px; object-fit: cover;">
          </div>
          <p id="previewDescripcion">Aquí irá la descripción del producto...</p>
          <p><strong>Tipo:</strong> <span id="previewType">---</span></p>
          <h5 class="text-end text-success" id="previewPrecio">$0.00</h5>
        </div>
      </div>
    </div>
  </main>

<%- include('../partial/footer.ejs') %>
<script src="agregar.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>